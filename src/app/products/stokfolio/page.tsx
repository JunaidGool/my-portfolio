// src/app/products/stokfolio/page.tsx
import React from "react";

export default function StokFolioPage() {
  return (
    <main className="container py-12 space-y-6">
      <h1 className="text-3xl font-bold">StokFolio</h1>

      <p>
        StokFolio is an online cockpit for stokvels—collect contributions, view
        live balances, run votes, and export ledgers in one place.
      </p>

      {/* TODO: embed live demo iframe + Loom once ready */}
    </main>
  );
}
